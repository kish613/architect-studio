# Tools

- `paperclipai` CLI is installed and can access the local Paperclip instance via `/Users/kivateit/.paperclip/instances/default/config.json`.
- `qmd` is not installed in this environment; use direct file reads for PARA memory recall until it is available.
