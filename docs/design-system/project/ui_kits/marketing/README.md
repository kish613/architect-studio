# Architect Studio — Marketing UI Kit

Recreates the dark-mode landing experience from `client/src/pages/Home.tsx`. Surfaces: hero frame with animated photo grid, floating nav pill, stats strip, featured projects, footer.

## Files
- `index.html` — clickable demo
- `LandingNav.jsx`, `HeroFrame.jsx`, `StatsStrip.jsx`, `FeatureGrid.jsx`, `Footer.jsx`

Based on `client/src/components/landing/*` in `kish613/architect-studio`.
